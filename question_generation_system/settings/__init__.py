# Settings package — the active settings module is selected via the
# DJANGO_SETTINGS_MODULE environment variable.
#
# Available modules:
#   question_generation_system.settings.base  — shared/common settings
#   question_generation_system.settings.dev   — local development overrides
#   question_generation_system.settings.prod  — production overrides
#
# Default (manage.py / tests): question_generation_system.settings.dev
